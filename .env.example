## Example environment variables for local development
# Copy this to a local .env file and fill values as needed. Do NOT commit secrets.

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=kong

# Kong settings (example)
KONG_DATABASE=postgres

# FastAPI
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000
